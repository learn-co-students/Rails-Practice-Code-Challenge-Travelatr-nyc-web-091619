<h1>Here is <%= @blogger.name %> stats 👨🏻‍💻:</h1>
<% if @blogger.posts.any? %>
    <h3>Total number of likes: <%= @blogger.total_likes%></h3>
    <h3>Featured post:</h3>
    <%= link_to @featured_post.title, post_path(@featured_post)%>
<% else %>
    <h3><%= @blogger.name %> is new to blogging he doesn't have any stats yet.</h3>
<% end %>

